name: Deploy Supabase Edge Functions

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "supabase/functions/**"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1

      - name: Deploy analyze-post
        run: supabase functions deploy analyze-post --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy send-welcome-email
        run: supabase functions deploy send-welcome-email --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy send-cancellation-email
        run: supabase functions deploy send-cancellation-email --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy create-checkout-session
        run: supabase functions deploy create-checkout-session --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy stripe-webhook
        run: supabase functions deploy stripe-webhook --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy submit-contact-form
        run: supabase functions deploy submit-contact-form --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy initialise-onboarding-sequence
        run: supabase functions deploy initialise-onboarding-sequence --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy send-onboarding-email
        run: supabase functions deploy send-onboarding-email --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy process-onboarding-sequence
        run: supabase functions deploy process-onboarding-sequence --project-ref fdowzbqsytvzsaqaawds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
